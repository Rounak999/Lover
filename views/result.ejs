<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Love Percentage Result</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: linear-gradient(to right, #ff758c, #ff7eb3);
            font-family: 'Poppins', sans-serif;
            text-align: center;
            padding: 20px;
        }
        .card {
            background: white;
            padding: 20px;
            border-radius: 20px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            margin: auto;
            animation: fadeIn 1s ease-in-out;
        }
        .heart {
            color: red;
            font-size: 40px;
            animation: heartbeat 1.5s infinite;
        }
        @keyframes heartbeat {
            0% { transform: scale(1); }
            50% { transform: scale(1.3); }
            100% { transform: scale(1); }
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .btn {
            background: red;
            color: white;
            padding: 10px;
            border-radius: 10px;
            transition: 0.3s;
            display: inline-block;
            text-decoration: none;
        }
        .btn:hover {
            background: darkred;
            transform: scale(1.1);
        }
        .percentage {
            font-size: 50px;
            font-weight: bold;
            color: #ff4d6d;
        }
    </style>
</head>
<body>
    <div class="card">
        <h1 class="text-3xl font-bold">💖 Love Compatibility Score 💖</h1>
        <p class="text-gray-600">Your love percentage is:</p>
        
        <div class="heart mt-4">❤️</div>
        <p class="percentage"><%= value %>%</p>

        <p class="text-gray-600 mt-4">
            <%= value >= 80 ? "A perfect match! 💞" : 
               value >= 50 ? "A strong bond! 💖" : 
               "Love needs some work! 💔" %>
        </p>

        <a href="/" class="btn mt-4">Try Again</a>
    </div>
</body>
</html>
